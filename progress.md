Original prompt: 스핔이 게임들 캐릭터마다 새로 소리낼때 이전에 재생되던 소리는 끊기게 해줘
그리고 그건 캐릭터마다 따로 적용되도록
소리는 방향에 따라 2 채널 지원하도록

## Progress

- Started investigation for Speaki game audio behavior across Bumpercar Spiky and Raise Speaki clients.
- Updated both multiplayer clients to stop the current per-character sound before playing the next one, and to route remote character sounds through Web Audio stereo panning by listener/emitter direction.
- Verified both updated clients with `node --check`, `manage.py check`, `collectstatic`, and the web game Playwright smoke test for `/ko/sub/bumpercar-spiky/` and `/ko/sub/raise-speaki/`.
- Added per-sound-list random selection memory so folder-backed SFX avoid repeating the same URL consecutively when two or more files are available.
- Re-ran `node --check`, `manage.py check`, `collectstatic`, gunicorn restart, URL health checks, and Playwright smoke tests for both Speaki game pages after the non-repeating random SFX change.
- Current prompt: bumpercar-spiky, raise-speaki에서 본인 캐릭터와 보스 효과음만 들리고 다른 캐릭터 효과음이 안 들리는 버그.
- Updated both clients so remote non-NPC character SFX keep a low spatial-volume floor while NPC/boss SFX still use the existing full distance cutoff.
- Current prompt: /Volumes/HANPLANET_HDD/Hanplanet/ONScripter/하루우루 를 웹에서 실행 가능하게 만들기.
- Added Django ONScripter player route backed by OnscripterYuri WASM, generated a loose-file `_web` asset folder to avoid downloading the full `.nsa` archives, patched lazy-load case handling, added default cursor BMPs, and patched one web-incompatible string sprite in `0.txt` so the WASM runtime does not divide by zero.
- Verified `/ko/sub/onscripter/haruuru/` loads the title screen and enters the game after selecting the first menu item. Remaining note: `cannot open system.lua` is logged by the engine but non-fatal for this title.
- Current prompt: ONScripter web-ported files must live under `/Volumes/HANPLANET_HDD/Hanplanet/ONScripter`, and saves must be stored per user.
- Moved the web runtime dependency files into `/Volumes/HANPLANET_HDD/Hanplanet/ONScripter/_web_runtime`, served JS/WASM/JSZip from media URLs, and added server-backed save sync at `/ko/sub/onscripter/<game>/save.zip`.
- Save archives now write to `/Volumes/HANPLANET_HDD/Hanplanet/ONScripter/_web_saves/<game>/<user-or-session>/save.zip`; logged-in users use a stable user key, anonymous visitors use their Django session key.
- Current prompt: in-game text is not visible.
- Fixed the text rendering issue by converting all loose script text files in `하루우루_web` from CP949 to UTF-8 and switching OnscripterYuri to `--enc:utf8`; original scripts are preserved as `*.web-encoding-backup` and excluded from the manifest.
- Verified with `manage.py check`, manifest checks, and a headless Chrome CDP smoke test that the first in-game dialogue text is visible.
- Current prompt: make `/sub/onscripter` a Sub-style page where users can choose an ONScripter game.
- Added an ONScripter game selection view that reuses the existing Sub card UI, links to `/ko/sub/onscripter/haruuru`, and exposes `hanplanet:sub-category=game` so the parent `/sub` page groups it under games.
- Verified with `manage.py check`, Django test client checks, and local server `curl` checks that `/ko/sub/onscripter` renders the selection page, redirects from `/sub/onscripter/`, and keeps the existing game player route working.
- Current prompt: ESC in-game menu text is garbled, and top-left hover should show mute and volume controls.
- Replaced the garbled custom ESC menu sprite labels in `하루우루_web/0.txt` with font-safe ASCII labels (`TEXT`, `LOAD`, `SAVE`, `LOG`, `FAST`, `RESET`, `END`).
- Added a top-left hover audio overlay to `templates/fun/onscripter_player.html`; it persists mute/volume in localStorage, applies the setting to video playback, and routes SDL2 WebAudio output through a GainNode for volume control.
- Verified with `manage.py check`, inline JS syntax parsing, the develop-web-game Playwright smoke client, and targeted Playwright screenshots/state checks for the audio hover panel, mute/slider state, and the in-game ESC menu.
- Current prompt: treat the ONScripter game screen as normal page content with a top navbar, fix overly wide in-game text spacing, and make the player background follow the active theme.
- Converted the ONScripter player to extend `base.html`, placed the canvas/video inside a body-stage layout below the shared navbar, and made the stage resize against its content area instead of the full browser viewport.
- Normalized fullwidth spaces and punctuation in the web `하루우루_web` script files so in-game text spacing renders tighter; original versions are preserved as `*.web-spacing-backup` and excluded from the manifest.
- Added theme-aware page and stage background variables for the player and verified the light/dark layout with `manage.py check`, inline JS syntax parsing, manifest checks, and targeted Playwright layout/theme screenshots.
- Current prompt: main menu item hit areas only work at the left edge, and `ui-nav-links` are not visible in the navbar.
- Restored the main menu hidden button hitbox strings in `하루우루_web/0.txt` and `하루우루_web/시노루트0.txt` to their original fullwidth-space width while leaving narrative text spacing normalized.
- Updated the ONScripter player so auto-collapsed nav links remain visible on the player page, nav height changes trigger canvas rescaling, and engine init no longer forces an inline black body background over the active theme.
- Verified with `manage.py check`, inline JS syntax check, mobile/desktop Playwright layout checks, and a targeted Playwright click test that the main menu center click enters the game again.
- Production follow-up: checked `https://www.hanplanet.com/ko/sub/onscripter/haruuru`, confirmed the latest player HTML and updated `0.txt?v=1782440164749700349-90063` are served, ran `collectstatic --noinput`, restarted gunicorn, and verified HTTPS desktop center-click plus mobile `ui-nav-links` visibility with Playwright.
- Current prompt: port `/Volumes/HANPLANET_HDD/Hanplanet/ONScripter/kanoina` and use each ONScripter child game's representative image for metadata/cards.
- Extracted `kanoina` NSA archives into `/Volumes/HANPLANET_HDD/Hanplanet/ONScripter/kanoina_web`, copied required scripts/font/config, converted scripts to UTF-8 with original backups as `*.web-encoding-backup`, and registered the game with `image/title.png` as its representative thumbnail.
- Added the default cursor BMPs to `kanoina_web` and commented the web-only `nsa` directive in `kanoina_web/0.txt` so the loose-file port runs without browser console errors.
- Verified `kanoina` with `manage.py check`, `main.tests.PwaMetadataTests`, Django test-client manifest/meta checks, the develop-web-game Playwright client, and full-page Playwright screenshots for the title screen plus START into the first in-game scene. Note: the skill client's canvas-only capture is black for this WebGL canvas, but full-page screenshots show the rendered game correctly.
- Deployed the `kanoina` registration to production with `collectstatic --noinput` and `./scripts/restart_gunicorn_and_wait.py`; verified `https://www.hanplanet.com/ko/sub/onscripter/`, `https://www.hanplanet.com/ko/sub/onscripter/kanoina/`, production `index.json`, and production Playwright screenshots for title plus START.
- Current prompt: fix `kanoina` web text spacing, trailing `\` display, click-advance progression, and ONScripter mobile navbar collapse.
- Converted active `kanoina_web/0.txt` and `kanoina_web/1.txt` back to CRLF line endings; kept the polished Korean text but changed only visible text-line word spaces to CJK spaces so ONScripter renders spacing. Added `*.pre-*-backup` to ONScripter manifest excludes so script backups are not served as game files.
- Found that OnscripterYuri rendered both trailing `\` and trailing `@` as literal text for this UTF-8 Kanoina script. Removed the line-end markers from `1.txt` and moved the click wait into Kanoina's `*nend` subroutine, which every dialogue block already calls. Playwright full-page screenshots confirmed first click shows only the first line and each later click advances one text block without displaying marker characters.
- Removed ONScripter-player-specific CSS that hid the shared mobile nav toggler and forced collapsed links visible; the player now uses the common responsive navbar behavior again. Also centered `onsyuri_webinfo` vertically inside the ONScripter stage.
- Follow-up correction: the `*nend` workaround still left text accumulation in real use. Restored active `kanoina_web/0.txt` from `0.txt.pre-click-spacing-bugfix-backup` and active `kanoina_web/1.txt` from `1.txt.pre-korean-polish-backup`, then touched both active scripts so their media cache keys changed. Playwright full-page screenshots confirmed the restored original structure does not display trailing `\` and click advances from the first line to the second line normally. Do not re-run broad translation polish on Kanoina without a structure-preserving script-aware transformer.
- Current prompt: only fix Kanoina's machine-translated Korean sentences without changing the script structure.
- Rebuilt active `kanoina_web/1.txt` from the restored original plus the natural Korean text pass, applying changes only to display text lines while preserving CRLF, line count, trailing `\` markers, command lines, and file-name references. The previous active file is preserved as `1.txt.pre-safe-polish-backup`, and backup files remain excluded from the game manifest.
- Verified the updated Kanoina script with the Django manifest check and Playwright full-page screenshots for first, second, third, and later click states; text advances one block per click and no trailing `\` marker is rendered.
- Follow-up correction: the early broadcast/animation sentence was still too long for Kanoina's text renderer and could corrupt the `2주` glyph plus stall progression. Shortened that block to two script lines using fullwidth `２`, then verified the exact problem block and the next block with Playwright.
- Current prompt: restyle the ONScripter audio control as a borderless translucent popup with shadow, reduce extra right-side space, keep mobile collapsed by default, and avoid duplicate spacing when only the mute button is shown.
- Updated the audio control CSS to use the shared popup surface/shadow/filter tokens, reduced the expanded width from 190px to 150px, shortened the slider to 92px, limited hover expansion to fine-pointer devices, and made the collapsed mobile state `gap: 0` with a transparent button background. Added outside pointer handling so mobile focus closes when tapping away.
- Verified with `manage.py check`, Playwright computed-style checks for desktop hover plus mobile initial/tap/blur states, mobile screenshots, and the develop-web-game Playwright client with no console error file.
- Follow-up correction: another stall occurred at `피해자는 야마다 히데코, 5세`; the shared cause was halfwidth ASCII alphanumerics inside visible script text. Converted visible dialogue/narration bodies in active `kanoina_web/1.txt` from halfwidth ASCII letters/digits to fullwidth forms while preserving dialogue command prefixes, voice IDs, command lines, CRLF, line count, and trailing `\` markers. The pre-conversion active file is preserved as `1.txt.pre-fullwidth-visible-alnum-backup`; manifest checks confirm backup files are excluded. Playwright verified the `５세` block and subsequent dialogue advance without glyph corruption or stalling.
- Current prompt: replace the ONScripter loading text modal with a canvas-area loading spinner.
- Added `#onscripter_canvas_loader` over the game stage, hid the old `onsyuri_webinfo` by default, and changed the loader lifecycle so it remains visible through engine init/cursor/font loading and hides only after the first non-cursor visual asset loads. The spinner uses a small high-contrast circular surface instead of the old text modal.
- Verified with `manage.py check`, a Playwright delayed-image test confirming the spinner remains until visual assets load, full-page screenshots for loading/ready states, and the develop-web-game Playwright client with no console error file.
- Deployed the loading spinner change with `collectstatic --noinput` and `./scripts/restart_gunicorn_and_wait.py`; production Playwright confirmed `onsyuri_webinfo` stays hidden, the spinner is visible during delayed visual loading, and the spinner is hidden after the title visual loads.
- Follow-up correction: Haruuru stalled near `안녕하세요. 뀀` for the same class of OnscripterYuri UTF-8 text parsing issue, this time triggered by halfwidth ASCII punctuation in visible script text. Converted non-command visible ASCII punctuation/alphanumerics in active `하루우루_web/1.txt` and `하루우루_web/시노루트1.txt` to fullwidth forms while preserving command lines, dialogue prefixes, trailing wait markers, and script line counts; pre-change backups are excluded from the manifest. Manifest checks confirmed updated cache keys and Playwright verified the intro text no longer corrupts and advances through the next dialogue blocks.

- Current prompt: reuse the site-wide loading spinner for the ONScripter canvas loader and document the shared-component-first rule.
- Updated `templates/fun/onscripter_player.html` so the canvas loader uses the shared `site-loading-overlay` and `site-loading-spinner` classes, keeping only ONScripter-specific placement/theme variable overrides. Added the explicit shared UI reuse rule to `PROJECT_GUIDELINES.md`.
- Verified with `manage.py check`, local Playwright DOM/style checks, the develop-web-game smoke client, and production Playwright screenshots. Deployed with `collectstatic --noinput` plus gunicorn restart; production now reports `site-loading-spin` on the ONScripter loader and hides it after the first visual asset loads.

- Follow-up correction: the ONScripter loader still had page-specific spinner variables/background/shadow, so it did not truly match the shared spinner. Removed those custom visual overrides; the loader now relies on the global `site-loading-overlay` and `site-loading-spinner` CSS for size, color, background, and animation, keeping only `pointer-events: none` for the canvas overlay behavior.

- Current prompt: use the provided downloaded image as Haruuru metadata image.
- Downloaded the provided WebP to `/Volumes/HANPLANET_HDD/Hanplanet/ONScripter/하루우루_web/IMAGE/HARUURU_META.webp` and updated the Haruuru `thumbnail_path` to `IMAGE/HARUURU_META.webp`, so both the ONScripter game card and Haruuru player OG/Twitter metadata use it.

- Follow-up: made ONScripter player OG/Twitter image URLs absolute while keeping sub-card preset image URLs relative for in-page rendering. Verified local rendered Haruuru player HTML contains `HARUURU_META.webp` in the metadata and the ONScripter index card data.
- Deployed the shared-spinner correction plus Haruuru metadata image change with `collectstatic --noinput` and gunicorn restart. Production verification confirmed Haruuru OG/Twitter tags use the absolute `HARUURU_META.webp` media URL, the image serves as `image/webp`, ONScripter index card data uses the same image, and the canvas loader uses the shared spinner defaults (`38px`, `3px`, `site-loading-spin`, no custom shadow/background on the spinner).

- Current prompt: port `/Volumes/HANPLANET_HDD/Hanplanet/ONScripter/konosora`, fix translationese where found, and check halfwidth text risks.
- Extracted `konosora` NSA archives into `/Volumes/HANPLANET_HDD/Hanplanet/ONScripter/konosora_web` using a direct NSA parser after confirming all archive entries are uncompressed. Copied `0.txt`, `1.txt`, `default.ttf`, and default web cursor BMPs.
- Converted scripts from CP949 to UTF-8 with `.web-encoding-backup` backups, commented the `nsa` directive for loose-file web loading, repaired the three invalid CP949 byte contexts in `1.txt`, converted visible text ASCII alphanumerics/punctuation to fullwidth forms to avoid OnscripterYuri UTF-8 parsing stalls, and applied a conservative visible-text polish pass for clear machine-translation fragments in the opening script.
- Registered `konosora` in `main/onscripter_views.py` with `image/title.png` as the representative image and UTF-8 runtime args.
- Follow-up correction: regenerated active `konosora_web/1.txt` from the CP949 backup with normal ASCII spaces in visible Korean text because non-ASCII spaces (`U+3000`, `U+2006`, `U+2009`, `U+00A0`) render as excessive fullwidth gaps in OnscripterYuri, while printable visible ASCII letters/digits/punctuation are still converted to fullwidth. Also converted visible choice labels and `0.txt` ESC-menu labels to Korean to avoid halfwidth menu glyph corruption.
- Verified `konosora` locally and in production: `manage.py check` passes, index JSON exposes 29,330 loose files with no `.nsa`/save/backup files, the ONScripter game list and player metadata use `konosora_web/image/title.png`, title/first-text/ESC-menu Playwright screenshots render correctly, and production is deployed via `collectstatic --noinput` plus gunicorn restart.

- Current prompt: port `/Volumes/HANPLANET_HDD/Hanplanet/ONScripter/그리자이아의 과실`, fix translationese where found, and check halfwidth text risks.
- Confirmed `그리자이아의 과실` uses ten uncompressed NSA archives (`arc.nsa` through `arc9.nsa`), then extracted 107,412 loose files into `/Volumes/HANPLANET_HDD/Hanplanet/ONScripter/그리자이아의 과실_web`. Copied `0.txt`, `1.txt`, `default.ttf`, and the default web cursor BMP files.
- Converted active `0.txt` and `1.txt` to UTF-8 with `.web-encoding-backup` backups, commented the `nsa` directive for loose-file web loading, translated the ESC `rmenu` labels to Korean, repaired the 12 CP949 replacement contexts in visible text, preserved `[speaker]` tag syntax for `gettag`, normalized visible non-ASCII spaces to normal spaces, converted visible printable ASCII to fullwidth forms, and polished the opening machine-translation-heavy lines. Also split five malformed source lines where an `lsp` command and visible text had been joined.
- Registered `grisaia-kajitsu` in `main/onscripter_views.py` using extracted `title.png` as the card/meta image and added `thumbs.db` to ONScripter manifest excludes. Local verification: `manage.py check` passes, index JSON exposes 107,417 files with no `.nsa`/save/backup/Thumbs DB entries, the standard develop-web-game client completed, and full-page Playwright screenshots confirmed the title screen, first in-game text, and Korean ESC menu render correctly.
- Deployed `grisaia-kajitsu` with `collectstatic --noinput` plus gunicorn restart. Production verification confirmed the ONScripter list card and player OG/Twitter metadata use the extracted `title.png`, production index JSON exposes 107,417 files with the expected UTF-8 runtime args and no excluded files, and production Playwright screenshots show the title screen, first in-game text, and Korean ESC menu.

- Current prompt: port `/Volumes/HANPLANET_HDD/Hanplanet/ONScripter/별하늘에 걸린 다리`, fix translationese where found, and check halfwidth text risks.
- Confirmed `별하늘에 걸린 다리` uses two uncompressed NSA archives (`arc.nsa`, `arc1.nsa`), then extracted 27,412 loose files into `/Volumes/HANPLANET_HDD/Hanplanet/ONScripter/별하늘에 걸린 다리_web`. Copied `0.txt`, `1.txt`, `default.ttf`, and the default web cursor BMP files.
- Converted active scripts to UTF-8 with `.web-encoding-backup` backups, commented the `nsa` directive, normalized visible non-ASCII spaces to normal spaces, converted visible printable ASCII to fullwidth characters, polished early machine-translation-heavy lines and choice labels, and disabled missing `movie/*.mjpg` calls because the source folder has no movie assets.
- Registered `hoshizora` in `main/onscripter_views.py` using the extracted title menu image `image/menu.jpg` as the card/meta image. Verified locally with `manage.py check`, `main.tests.PwaMetadataTests`, Django manifest/meta checks, the develop-web-game smoke client, and full-page Playwright screenshots for title, first text, and Korean ESC menu.
- Deployed `hoshizora` with `collectstatic --noinput` plus gunicorn restart. Production verification confirmed `https://www.hanplanet.com/ko/sub/onscripter/hoshizora/`, the ONScripter list card, player OG/Twitter metadata, index JSON with 27,420 served files and no `.nsa`/save/backup entries, and HTTPS Playwright title/first-text/ESC-menu screenshots with no console error/pageerror.

- Current prompt: make ONScripter child-game browser tab titles match their meta titles.
- Added an ONScripter-player-only document title guard that keeps `document.title` equal to `meta_title` even after OnscripterYuri calls `emscripten_set_window_title` and overwrites it with `ONScripter`. Expanded metadata tests to cover every registered ONScripter game title/description and the tab-title guard value.
- Verified with `manage.py check`, `main.tests.PwaMetadataTests`, the develop-web-game smoke client, local Playwright title checks, `collectstatic --noinput`, gunicorn restart, production HTML checks for Haruuru/Hoshizora, and production Playwright title checks confirming runtime and forced-overwrite recovery both return `별하늘에 걸린 다리 | Hanplanet ONScripter`.

- Current prompt: set Hoshizora's ONScripter meta title and browser tab title to `hoshizora`, then fix the ONScripter `site-loading-host` spinner background staying white in dark mode.
- Added a per-game ONScripter `meta_title` override and set only `hoshizora` to `hoshizora`, leaving the visible game title/list label as `별하늘에 걸린 다리`. The document-title guard now uses that override, so runtime title recovery also restores `hoshizora`.
- Updated the ONScripter stage to feed the shared loading overlay/spinner with stage/theme-scoped variables (`--site-loading-overlay-bg`, `--site-loading-spinner-track`, `--site-loading-spinner-accent`) so dark mode uses the dark game-stage surface instead of the shared light modal surface. Verified locally with `manage.py check`, `main.tests.PwaMetadataTests`, HTML checks, Playwright computed-style checks, a delayed dark-loading screenshot, and the develop-web-game smoke client.

- Current prompt: check missing spaces in Konosora, Hoshizora, and Grisaia in-game text against originals, then use an external Korean spacing checker if the originals are also unspaced.
- Compared original `1.txt` scripts with the web ports. The originals mostly use fullwidth spaces (`U+3000`), especially around punctuation, rather than being fully unspaced. The web ports had lost many of those punctuation-adjacent spaces while converting away from fullwidth gaps.
- Installed and tested KSS `correct_spacing` as the external Korean spacing checker. It correctly suggested fixes for simple stuck phrases such as `무슨일이야`, but it also over-split names/onomatopoeia (`나카츠가와우이`, repeated cries), so it was used only to guide manually reviewed safe replacements instead of being applied blindly.
- Updated `/Volumes/HANPLANET_HDD/Hanplanet/ONScripter/konosora_web/1.txt`, `/Volumes/HANPLANET_HDD/Hanplanet/ONScripter/별하늘에 걸린 다리_web/1.txt`, and `/Volumes/HANPLANET_HDD/Hanplanet/ONScripter/그리자이아의 과실_web/1.txt`: restored readable ASCII spaces after Korean/fullwidth punctuation, fixed common stuck spacing patterns (`무슨 일`, `안 돼`, `것 같`, etc.), and manually corrected `나카츠가와 우이` cases. Backups were saved as `1.txt.pre-spacing-checker-backup` and remain excluded by the manifest.
- Verification: `manage.py check`, `main.tests.PwaMetadataTests`, local and production `index.json` checks confirmed the three updated `1.txt` cache keys and no backup files in manifests; develop-web-game smoke loads and full-page Playwright captures for Konosora/Hoshizora/Grisaia completed without console errors.

- Current prompt: try displaying fullwidth visible script text as halfwidth only in browser via the font-patch approach, then fix the regression where in-game text line wrapping/newlines disappeared.
- Tested a display-only font cmap patch for every registered ONScripter web port by mapping `U+FF01`-`U+FF5E` and `U+3000` to their ASCII glyphs in each `_web/default.ttf`, with `default.ttf.pre-halfwidth-display-backup` backups excluded from the manifest. The patch changed glyph advance widths too, so ONScripterYuri's text layout fit formerly wrapped text onto fewer lines across games.
- Rolled the font patch back immediately by restoring all five active `_web/default.ttf` files from `default.ttf.pre-halfwidth-display-backup` and touching them so production `index.json` serves new cache keys. Verified the restored cmap maps fullwidth characters back to their original fullwidth glyphs, backup fonts remain excluded from local/production manifests, and Haruuru still loads in production after the rollback. Do not reapply global cmap width remapping; any future halfwidth-display attempt must preserve ONScripter line-breaking metrics or patch the engine renderer after layout.

- Current prompt: the missing ONScripter in-game spacing was spaces, not newlines; confirm script spaces and fix them.
- Confirmed active scripts still contained spaces: Kanoina used mostly fullwidth spaces (`U+3000`) and rendered visibly, while Haruuru/Konosora/Hoshizora/Grisaia mostly used ASCII spaces (`U+0020`) that OnscripterYuri did not visibly render in dialogue. First attempted converting visible story spaces to `U+3000` and reducing `U+3000` font width, but the user reported the spaces were still too wide; likely the engine treats `U+3000` as a full CJK cell separately from font hmtx in the text layout path.
- Reworked the fix to avoid `U+3000` for dialogue spacing: restored `U+3000` font width from `default.ttf.pre-u3000-space-width-backup`, mapped private-use `U+E000` to the existing ASCII space glyph in every active web font, converted visible story-text spaces in Haruuru/Konosora/Hoshizora/Grisaia `1.txt` files to `U+E000`, and restored Haruuru menu hitbox spacer strings to seven `U+3000` characters. Kanoina script text remains unchanged as the known-good reference.
- Verification: `manage.py check` passed; local and production `index.json` show new font/script cache keys with no `*.pre-*-backup` files; fontTools confirms `U+E000` maps to each font's ASCII space glyph while `U+3000` keeps original fullwidth metrics; Pillow/FreeType measurements confirm `가나다` equals `가 나 다` width and is much narrower than `가　나　다`. Playwright smoke against production Konosora exits 0, though automated title-menu clicks remain unreliable for reaching dialogue screenshots.

- Follow-up prompt: the script spaces are visible but still too wide; make them about half the current size.
- Updated all active web fonts so `U+E000` maps to an invisible blank glyph with half the ASCII-space advance width instead of sharing the full ASCII-space glyph. Haruuru uses unmapped `glyph17145`, Kanoina/Konosora/Grisaia use blank `glyph08094`, and Hoshizora uses `.null` plus patched `hdmx` device widths so FreeType honors the half-width advance.
- Converted Kanoina's visible story-text `U+3000` spaces to the same `U+E000` spacing model for consistency with the other games. Verification: `manage.py check` passed; local and production manifests expose updated cache keys and exclude all backups; Pillow/FreeType confirms `가나다` is now halfway between no-space and ASCII-space text for all five games, with zero rendered pixels for the spacer; develop-web-game production smoke exited 0 for all five games.

- Follow-up correction: the user reported no visual difference from the `U+E000` half-width spacer, meaning OnscripterYuri likely still treated the private-use codepoint as a full-width non-ASCII cell internally. Switched the visible story spacer to ASCII backtick (`U+0060`) so the engine uses its ASCII/half-width layout path, then remapped `U+0060` in every active web font to the existing invisible half-width spacer glyph. Active scripts now contain backticks as word spacers and no `U+E000` spacers.
- Verification: `manage.py check` passed; fontTools/Pillow confirm backtick renders zero pixels and advances at half ASCII-space width; local and production manifests expose new font/script cache keys with all backup files excluded; develop-web-game production smoke exited 0 for all five games. If this still appears unchanged, the next fix must be a runtime/engine text-layout patch rather than any script/font-only substitution.

- Follow-up correction: Konosora froze immediately after START with a corrupted `쀀` glyph. The ASCII backtick spacer was likely being interpreted by OnscripterYuri's script parser, so all active story spacer backticks were replaced with `U+FF65` and every active web font maps `U+FF65` to the same invisible half-width spacer glyph.
- Verification: active scripts now contain no backticks or `U+E000` spacers; fontTools/Pillow confirm `U+FF65` renders zero pixels and advances at the intended half-space width; `manage.py check` passes; local and production manifests expose matching updated cache keys with no backup/archive files. Production Playwright confirmed Konosora reaches the first and second in-game text screens after START without the `쀀` freeze.

- Current prompt: switch the ONScripter spacing fix to an engine pre-render conversion path.
- Rebuilt the active OnscripterYuri `v0.7.7beta` web runtime from source with a small `ONScripter_text.cpp` patch: when the renderer is about to draw UTF-8 `U+FF65`, it now skips glyph drawing, advances the cursor by one hankaku cell, and preserves the original bytes for lookback text. This keeps the script parser stable while making the spacer half-width at the engine rendering boundary.
- Stored the reproducible patch at `/Volumes/HANPLANET_HDD/Hanplanet/ONScripter/_web_runtime/onsyuri/0.7.7beta/patches/hanplanet-halfwidth-spacer.patch`, documented the rebuild in the runtime `README.md`, and kept the previous runtime as `onsyuri.js.pre-render-spacer-backup` and `onsyuri.wasm.pre-render-spacer-backup`.
- Verification: `manage.py check` passes; production HTML references the rebuilt `onsyuri.js?v=1782485336959089000-199340` and `onsyuri.wasm?v=1782485336997665000-4587455`; production Playwright screenshots confirmed Konosora's first and second in-game text screens render with visibly narrower spaces and no `쀀` freeze; develop-web-game production smokes exited 0 for Haruuru, Kanoina, Konosora, Hoshizora, and Grisaia.

- Current prompt: Konosora still has many translationese lines in the script.
- Ran a targeted Korean polish pass on active `/Volumes/HANPLANET_HDD/Hanplanet/ONScripter/konosora_web/1.txt`, limited to visible script text. The pass removed high-frequency machine-translation leftovers such as `무엇인가`, `어째서`, `이것으로부터`, `나의`, `우리들의`, `것이다`, `라고의`, and fixed name/josa corruption such as `파랑에` -> `아오이`, `호타루과` -> `호타루와`, and `토비오카이` -> `토비오카가`.
- Also manually rewrote early visible Konosora scenes around the secret-base/activity setup, Hotaru walking-to-school sequence, and model-glider scene where the script had obvious broken phrasing. The pre-pass script is preserved as `1.txt.pre-translationese-pass-backup`, and manifest exclusion keeps that backup out of the served game files.
- Verification: `manage.py check` passes; production `index.json` serves the new `1.txt?v=1782486483061290689-7521110` and excludes `pre-translationese` backups; production develop-web-game smoke exits 0; full-page Playwright confirmed the title screen, START into the first in-game text, and next text advance with zero console/page errors.

- Current prompt: ONScripter navbar overlaps the in-game audio/volume control and makes volume adjustment impossible.
- Updated `templates/fun/onscripter_player.html` so the audio panel uses the shared floating z-index instead of local `z-index: 30`, and the navbar auto-reveal pointer handler now treats the expanded audio panel hit area as an exclusion zone. While the pointer is over the mute/volume control, the navbar reveal class is removed instead of covering the control.
- Verification: `manage.py check` passes; local develop-web-game smoke exits 0; local and production Playwright hover checks confirm `#onscripter_audio_volume` is the top hit target at the slider position, `pointer-events` is `auto`, and the navbar stays hidden while the audio panel is open. Restarted production gunicorn so the template change is live.

- Current prompt: place `onscripter_audio_volume` below `onscripter_audio_mute`, make the volume bar vertical, and move the control to the canvas top-right.
- Updated `templates/fun/onscripter_player.html` so the audio panel is a 44px-wide vertical column, expands downward to reveal a vertical range input under the mute button, and positions itself from the actual canvas rect via CSS variables. The position is recalculated on initial setup, viewport resize, stage/nav resize, and canvas resize so loading-time canvas size changes do not leave the panel at the stage edge.
- Verification: `manage.py check` passes; local develop-web-game smoke exits 0; local and production Playwright checks confirm early/loading and final canvas states both keep the panel at canvas right/top (`rightDelta` 0, hover `topDelta` 0), the vertical slider is `32px x 92px` with `writing-mode: vertical-lr`, `#onscripter_audio_volume` is the top hit target when open, and mobile production starts collapsed then opens the vertical slider only after tapping the mute button. Restarted production gunicorn so the template change is live.

- Current prompt: make `onscripter_audio_panel` background translucent like the other modals again.
- Updated `templates/fun/onscripter_player.html` so the audio panel uses the shared popup/modal surface tokens directly (`--site-dropdown-surface-bg` / `--site-modal-surface-bg`) with `background-image: none`, and removed the hover/focus `color-mix(... 90%)` overrides that made the expanded panel look opaque.
- Verification: `manage.py check` passes; local and production computed-style checks confirm light mode uses `rgba(248, 248, 248, 0.72)` and dark mode uses `rgba(46, 46, 46, 0.72)` before and during hover, with the vertical volume input still clickable. Local/production screenshots were visually inspected, production develop-web-game smoke exits 0, and gunicorn was restarted so the template change is live.

- Current prompt: add a canvas top-left rotate button; clicking rotates the ONScripter canvas clockwise 90 degrees and clicking again restores it, with the rotate button and audio controls moving with the canvas.
- Updated `templates/fun/onscripter_player.html` so `#onscripter_rotate_toggle` and `#onscripter_audio_panel` are overlays inside `#onscripter_canvas_frame` instead of separate stage overlays. The frame rotation now carries the controls with the canvas surface: the local top-left rotate button moves to the visual top-right after clockwise rotation, and the local top-right audio panel moves to the visual bottom-right. Removed the previous viewport-edge repositioning and per-control rotation overrides that made controls appear fixed in place.
- Verification: `manage.py check` passes; local and production develop-web-game smokes exit 0; local and production Playwright coordinate checks confirm the controls are frame children, the frame is transformed, the rotate button switches to the counterclockwise state, and the volume slider remains the top hit target after rotation on desktop and mobile. Local and production screenshots visually confirm the controls follow the rotated canvas. Restarted production gunicorn so the template change is live.

- Current prompt: fix the ONScripter navbar staying visible after moving the cursor away from the top reveal area until the cursor is moved over it again.
- Updated `templates/fun/onscripter_player.html` so the navbar auto-hide logic stores the latest pointer coordinates and decides whether to keep the nav visible from the actual pointer position/focus state instead of relying on potentially stale `:hover` state. Removed the CSS-only `.ui-nav:hover` reveal selector so visibility is driven by the JS reveal state and `:focus-within`.
- Verification: `manage.py check` passes; local and production develop-web-game smokes exit 0; local and production Playwright pointer scenarios confirm the navbar reveals at the top, drops `is-navbar-revealed` after one cursor leave, and is fully hidden after the transition without requiring a second hover. Restarted production gunicorn so the template change is live.

- Current prompt: show the ONScripter card on the Sub page only to users allowed in admin, always show it to admins, and restrict ONScripter pages/subgames to allowed users.
- Added `OnscripterAccessUser` with admin registration (`ONScripter 허용 사용자`). Users listed there can see/access ONScripter; `is_staff` or `is_superuser` users are allowed even without a row. The Sub page now filters out the `onscripter` card for anonymous/unallowed users, and ONScripter index/player/index.json/save.zip routes all share the same access check.
- Verification: `manage.py check`, `makemigrations --check --dry-run`, and targeted ONScripter access tests pass. Applied migration `main.0048_onscripteraccessuser`, restarted production gunicorn, and verified production anonymous `/ko/sub/` contains no ONScripter card while `/ko/sub/onscripter/`, `/ko/sub/onscripter/haruuru/`, `/ko/sub/onscripter/haruuru/index.json`, and `/ko/sub/onscripter/haruuru/save.zip` return 403. A broader existing Sub-page test command still has unrelated failures from stale expectations (`Hanplanet Wargame`/`sub_link_groups["items"]`), not from this change.

- Current prompt: add a gear settings button to the left of the ONScripter volume control that triggers ESC/menu, and fix main-menu clicks while the canvas is rotated 90 degrees.
- Started updating `templates/fun/onscripter_player.html`: added a top-right canvas control group with a gear button next to the audio panel, wired the gear button to the existing ESC menu path, added a canvas-rotation mouse-coordinate remapper for rotated canvas clicks, and exposed a small `render_game_to_text` UI-state hook for Playwright verification.
- Early verification: `manage.py check`, rendered-player inline JS `node --check`, and `main.tests.PwaMetadataTests` all pass.
- Finalized the ONScripter player update: the gear button now uses a more complete synthetic ESC `KeyboardEvent` (`key`, `code`, `keyCode`, `which`) so OnscripterYuri opens the in-game menu; rotated canvas mouse input is remapped before the engine sees it, and rotated touch input now emits mapped mouse events. Local Playwright verified the gear button opens the ESC menu and 90-degree rotated main-menu clicks enter the game; the required develop-web-game client also ran successfully. Restarted production gunicorn and verified production HTML plus HTTPS Playwright screenshots for rotated main-menu click and rotated in-game gear menu. The only console error observed remains the existing non-fatal loose-file `nsa/ns2 archive` warning.

- Current prompt: swap the ONScripter settings and volume controls, make the settings icon look more polished, and replace the 90-degree rotate icon that looked like a back button.
- Updated `templates/fun/onscripter_player.html` so the canvas top-right controls now order the audio panel before the settings button, replaced the settings glyph with a fuller gear outline, and replaced the rotate button glyphs with screen-orientation rotation icons. Also separated rotate/settings icon sizing so the orientation icon reads more clearly in the canvas control button, and restored the collapsed audio panel opacity so the mute button remains visible while the volume slider stays hidden until hover/focus.
- Follow-up: replaced the custom settings gear with the balanced lucide-style settings path because the old outline made the center circle look visually off-center.
- Current prompt: convert the provided two-gear PNG into SVG and use it for the site-wide settings buttons, including `ui-auth-account-admin-icon` and `ui-auth-account-weather-settings-icon`.
- Added `static/media/icons/settings-gears.svg` traced from the provided transparent PNG. Updated the account widget weather settings icon and admin icon to use that SVG as their CSS mask, and updated the ONScripter in-game settings button to use the same SVG path inline with `currentColor`.
- Verification/deploy: `manage.py check`, rendered ONScripter inline JS `node --check`, `main.tests.PwaMetadataTests`, and the required develop-web-game smoke all pass. Ran `collectstatic --noinput`, restarted gunicorn, and verified production serves `settings-gears.svg` as `image/svg+xml`; production account admin, account weather settings, and ONScripter settings icons all reference/render the new SVG.

- Current prompt: use the provided rotation icon sheet and crop each angle for the ONScripter rotate button.
- Cropped `/Volumes/HANPLANET_HDD/Hanplanet/media/HanDrive/users/admin/로테.png` into transparent mask PNG assets: `static/media/icons/rotate-90.png`, `rotate-180.png`, `rotate-270.png`, and `rotate-360.png`. Updated the ONScripter rotate button to show the 90-degree icon before rotation and the 360-degree icon after rotation/restoring, using the cropped images as `currentColor` CSS masks.
- Follow-up: regenerated the cropped rotation assets without any extra bottom labels or underline/crop-edge artifacts; only the original in-icon angle marks remain.
- Verification/deploy: `manage.py check`, rendered ONScripter inline JS `node --check`, `main.tests.PwaMetadataTests`, and the required develop-web-game smoke pass. Ran `collectstatic --noinput`, restarted gunicorn, confirmed production serves all four `rotate-*.png` assets as `image/png`, and verified production browser rendering switches from `rotate-90.png` to `rotate-360.png` without bottom labels/underlines.
- Follow-up: increased only the visible ONScripter rotate icon mask size by 30%, from `28px` to `36.4px`, while keeping the `44px` button box unchanged. Added a fixed flex basis so the icon is not squeezed back to the button's 32px content box.
- Verification/deploy: local and production Playwright computed styles confirm the rotate button remains `44px x 44px` while the visible icon is `36.4px x 36.4px` before and after rotation. Required develop-web-game smoke still passes, and gunicorn was restarted for production.
- Current prompt: after rotating 90 degrees, show the 270-degree rotate icon instead of the 360-degree icon, and fix the default volume icon sitting higher than the other icons.
- Updated the rotated-state ONScripter rotate icon from `rotate-360.png` to `rotate-270.png`. Removed the default `-4px` Y offset from `#onscripter_audio_panel` so the collapsed volume button aligns vertically with the settings button.
- Verification/deploy: `manage.py check`, rendered ONScripter inline JS `node --check`, `main.tests.PwaMetadataTests`, and the required develop-web-game smoke pass. Local and production Playwright confirm the audio/settings top delta is `0`, the audio transform is neutral, and the rotated-state icon mask is `rotate-270.png`. Restarted gunicorn for production.
- Current prompt: hide ONScripter canvas buttons before loading completes because the early tiny canvas makes controls cluster in the middle.
- Updated `templates/fun/onscripter_player.html` so rotate/audio/settings controls are hidden by default and become visible only after the existing canvas loader is hidden. The loader completion path now adds `is-onscripter-canvas-ready` to `#onscripter_canvas_frame`, and `render_game_to_text` reports `canvasControlsReady` for verification.
- Verification/deploy: `manage.py check`, rendered ONScripter inline JS `node --check`, `main.tests.PwaMetadataTests`, and the required develop-web-game smoke pass. Local and production Playwright delayed/held loading checks confirm controls stay hidden while the loader is visible, including the production 300x150 early canvas state, and become visible after `is-onscripter-canvas-ready`; restarted gunicorn for production.
- Current prompt: fix Haruuru memorial album internal button click ranges.
- Found the album bottom controls in `하루우루_web/0.txt` use hidden text sprites as hit targets. The converted page-number sprites used empty bracket text (`「」`) and the larger Music/Scene/Back sprites were offset from the visible JPG buttons, so page-edge clicks could hit the next page and Music center clicks could miss. Updated the album/scene/music bottom-control `lsph` hitbox sprites to use fullwidth-space hitboxes aligned to the visible buttons.
- While verifying the now-clickable Music button, the existing `*music` block stalled on malformed/garbled `lsp` list lines. Rewrote those visible music-list lines with valid comma-separated `lsp` syntax and safe Korean/fullwidth punctuation so the Music subpage can render after the button hitbox fix.
- Verification/deploy: `manage.py check`, `main.tests.PwaMetadataTests`, and the required develop-web-game smoke pass. Local and production full-page Playwright checks confirm the CG album page-number right edge stays on page 2 instead of jumping to page 3, Music center opens the Music album page, Scene center opens the Scene album page, and Back center returns to the main menu. Restarted gunicorn; production `index.json` and the served `0.txt` use the new cache key/content.
- Current prompt: Haruuru freezes when a choice should appear; identify cause and check other games for the same bug.
- Root cause: the fast Haruuru choice path in `하루우루_web/0.txt` and `시노루트0.txt` hit a visible dialogue line `「어느 쪽이야?」`; the halfwidth `?` is parsed by OnscripterYuri's UTF-8 script reader as syntax and produced `Script error, Array No. is not declared`, stopping before the choice UI. Replaced it with fullwidth `？` in both active Haruuru control scripts.
- Also hardened the shared choice-button routines: Haruuru, Kanoina, and Konosora now register the fixed selection-window image sprites (`25/27/29/31`) as `spbtn` hit targets instead of variable-width text sprites (`24/26/28/30`). Grisaia already uses image sprites for choices, and Hoshizora uses `exbtn` image selection grids, so they do not share this text-sprite hitbox risk.
- Verification: scans found no remaining `?」`/`!」` choice-adjacent lines or old `spbtn 24,1`/`26,2`/`28,3` choice registrations in the active public web scripts. `manage.py check`, `main.tests.PwaMetadataTests`, and the required develop-web-game smoke pass. Local full-page Playwright confirms Haruuru reaches the `어느 쪽이야？` line, displays the two choices, and selecting `오른쪽으로 간다` advances into the next branch with no new script error.
- Current prompt: hide the narrow temporary canvas and horizontal volume bar that briefly appear at the start of ONScripter subgame page loads.
- Root cause: the first-paint CSS that hides canvas overlays and turns the range input into a vertical volume slider lived late in `templates/fun/onscripter_player.html`, after body markup/scripts. During a slow `index.json` load the browser could paint native defaults first: a 300x150 canvas and a normal horizontal `<input type="range">`.
- Added critical first-paint CSS to the page head so `#canvas`, `#video`, rotate/audio/settings controls, and `#onscripter_canvas_controls` remain hidden until `#onscripter_canvas_frame` receives `is-onscripter-canvas-ready`; the early frame also reserves a stable 4:3 layout and pre-styles `#onscripter_audio_volume` as an invisible vertical slider.
- Verification/deploy: `manage.py check`, `main.tests.PwaMetadataTests`, local delayed-load Playwright, production delayed-load Playwright, and the required develop-web-game smoke all pass. Production HTML includes the new head critical CSS and gunicorn was restarted. The only console error observed is the existing non-fatal `failed to open nsa or ns2 archive, ignored` warning.
- Current prompt: add an ONScripter auto-mode toggle at the canvas bottom-right, and make it advance after dialogue text output and voice playback are both done plus 2 seconds instead of using a fixed interval.
- Added `#onscripter_auto_toggle` as a bottom-right canvas overlay that is hidden until the canvas is ready and follows the existing frame rotation behavior. The button toggles play/pause icons and exposes state through `render_game_to_text`.
- Auto advance now runs a 250ms monitor instead of a fixed click interval. It samples the lower-left dialogue text area to detect when text rendering has stabilized for 700ms, tracks lazy `voice/` file access and FS `open()` calls, decodes voice duration when the browser can, then dispatches the next-click only after `max(textDone, voiceDone) + 2000ms`. If voice duration cannot be decoded immediately, it uses a conservative 6.5s fallback until decode finishes.
- The auto-advance click is sent to the dialogue area's lower-right side instead of the canvas center, which keeps it away from most choice-button hitboxes while still advancing normal dialogue.
- Verification/deploy: `manage.py check`, rendered inline JS `node --check`, `main.tests.PwaMetadataTests`, local Playwright UI/rotation checks, a local dialogue auto-advance run, and the required develop-web-game smoke all pass. The local dialogue run confirms the first text advances to the next line after the text-stable + 2s window; synthetic voice-file access confirms `voice/aka_0118.ogg` is detected and duration decoding updates the auto wait. Restarted production gunicorn, verified production HTML includes the auto button and voice open tracker, and verified the production browser can toggle auto mode with `voiceTrackerInstalled: true`. Existing non-fatal `nsa/ns2 archive` console warning remains.
- Follow-up: text-only dialogue lines should not wait for stale voice timing. Auto-advance now tracks voice timing per current dialogue cycle; if no `voice/` resource opens during the current cycle, the line advances after text-stable + 2s. Manual canvas clicks while auto mode is enabled also reset the cycle so subsequent text-only lines do not inherit the previous line's voice wait.
- Current prompt: remove the ONScripter auto button's active background-color change, add a skip button to the left of auto, and make skip behave like the native Ctrl fast-mode only while held.
- Updated `templates/fun/onscripter_player.html`: the auto button keeps the same translucent popup background while active/hovered/focused, and the new bottom-right playback control group places `#onscripter_skip_toggle` immediately left of `#onscripter_auto_toggle`.
- The skip button now sends Ctrl keydown on pointer/keyboard press and Ctrl keyup on release/cancel/blur, so it is hold-only rather than a toggle. Enabling skip turns auto mode off, and manual canvas advance still clears skip/auto cycle state. The legacy web menu skip button uses the same hold handlers.
- Verification/deploy: `manage.py check`, ONScripter inline JS `node --check`, and `main.tests.PwaMetadataTests` pass. Restarted gunicorn and verified production with Playwright: auto active background remains `rgba(248, 248, 248, 0.72)`, skip appears left of auto, skip is enabled only while held and disables on release, and skip hold disables auto. The required develop-web-game client also ran through an authenticated local proxy; its canvas-only screenshot remains black due the existing WebGL capture limitation, while full-page Playwright screenshots confirm the overlay controls are visible.
- Current prompt: Haruuru freezes after `그걸 듣고 기뻐할 메이드는 없을 테지？`; determine whether a halfwidth bug remains and re-check all ONScripter games if so.
- Root cause confirmed: the line itself already used fullwidth `？`, but the next text tag speaker name was `성이･아카츠키?･`, leaving a halfwidth `?` inside a visible speaker-name tag. That still goes through the text/tag parser and could blank the message window / stall after the previous line.
- Applied a visible-text-only halfwidth audit/fix across active web scripts. Converted halfwidth ASCII only in speaker names, tag text bodies, continuation text, and bare visible dialogue/narration lines; command lines and voice IDs were left untouched. Changed active files: `하루우루_web/1.txt`, `하루우루_web/시노루트1.txt`, `kanoina_web/1.txt`, and `konosora_web/1.txt`. Backups were saved as `*.pre-visible-halfwidth-risk-backup` and are excluded from game manifests.
- Also fixed a separate Konosora structural typo at `konosora_web/1.txt:153400`, changing malformed `[1|아오이nend` back to `nend`.
- Verification: the refined scanner reports `0` visible halfwidth-risk lines for Haruuru, Kanoina, Konosora, Hoshizora, and Grisaia; the text-tag structure checker passes; `manage.py check` and `main.tests.PwaMetadataTests` pass; all game manifests exclude the new backups. Production `index.json` and served Haruuru `시노루트1.txt` show line 1611 as `성이･아카츠키？･`. The required develop-web-game client loaded Haruuru through an authenticated proxy; its canvas-only screenshot remains black from the known WebGL capture limitation, while full-page Playwright loaded production with no new console/page errors.
- Current prompt: stack the ONScripter playback controls vertically with skip above and auto mode below.
- Updated `templates/fun/onscripter_player.html` so `#onscripter_bottom_controls` uses a column flex direction. The existing DOM order already places `#onscripter_skip_toggle` before `#onscripter_auto_toggle`, so the visual order is skip on top and auto on the bottom-right.
- Verification/deploy: `manage.py check` and `main.tests.PwaMetadataTests` pass. Restarted production gunicorn and verified `https://www.hanplanet.com/ko/sub/onscripter/haruuru/` with Playwright: `#onscripter_bottom_controls` computes to `flex-direction: column`, skip is above auto, both buttons share the same X position, and the auto button remains 10px from the canvas bottom/right. The required develop-web-game client also passes through the authenticated local proxy; its canvas-only screenshot remains black due the existing WebGL capture limitation, while the full-page screenshot confirms the vertical button layout.
- Current prompt: fix Haruuru voice playback not working.
- Investigation: Haruuru scripts request voices as lowercase paths such as `voice/mis_0001.ogg`, while extracted files are stored as uppercase paths such as `VOICE/MIS_0001.OGG`. Manual in-page `fetch_file()` with the lowercase path succeeds, but the lazy loader only looked up the exact path string passed from the engine, so any engine-side case variation could miss the manifest entry. The page also only resumed SDL AudioContext from overlay controls, not ordinary canvas/gameplay clicks, which can leave real browsers blocked by autoplay policy.
- Updated `templates/fun/onscripter_player.html` so `fetch_file()` falls back to a lowercase filemap lookup and added document-level user-gesture handlers (`pointerdown`, `mousedown`, `touchstart`, `keydown`) that resume the SDL AudioContext and reapply the site volume state.
- Follow-up hardening: `fetch_file()` now records both the original mounted path and the lowercase lookup alias into MEMFS when lazy-loading a file, and copies an already-loaded mounted file to a newly requested case variant if needed.
- Verification/deploy: `manage.py check`, `main.tests.PwaMetadataTests`, and rendered inline JS `node --check` pass. Restarted production gunicorn and verified production Haruuru with Playwright: calling `fetch_file()` for `/onsyuri/haruuru/VOICE/AKA_0118.OGG` succeeds through the lowercase filemap entry, both upper/lower MEMFS aliases have the expected 9077-byte OGG file, the media response is `200 audio/ogg`, the voice tracker sees `/onsyuri/haruuru/voice/aka_0118.ogg`, SDL AudioContext is `running`, and site volume gain remains 0.8. Required develop-web-game client passes through the authenticated proxy; only the existing non-fatal `nsa/ns2 archive` console warning appears.
- Current prompt: keep the ONScripter settings button background changed only while hovering, not after clicking.
- Updated `templates/fun/onscripter_player.html` so the settings button blurs immediately after opening the in-game menu, and its `:focus-visible` background is pinned to the default translucent surface instead of the hover tint.
- Verification/deploy: `manage.py check`, `main.tests.PwaMetadataTests`, and rendered inline JS `node --check` pass. Restarted production gunicorn and verified production Haruuru with Playwright: settings button background is `rgba(248, 248, 248, 0.72)` initially, changes only on hover, then returns to the same default background after click and pointer leave with no lingering active/focus state. Required develop-web-game client passes through the authenticated proxy with only the existing non-fatal `nsa/ns2 archive` warning.

- Follow-up correction: production was serving the prior settings/voice changes, but two gaps remained. Touch browsers can keep `:hover` matched after a tap, so the ONScripter canvas button hover tint now applies only under `(hover: hover) and (pointer: fine)`, with focus-visible kept on the default translucent surface.
- Haruuru voice follow-up: enabled Haruuru-only `directVoicePlayback` in the generated game index and added a direct WebAudio fallback for `VOICE/*` resources. The fallback resolves absolute, relative, and lowercased lazy-file paths, stops any previous voice before starting the next one, and uses the site volume/mute state. Other ONScripter games keep `directVoicePlayback: false` to avoid duplicate voice playback.
- Verification/deploy: `manage.py check`, `main.tests.PwaMetadataTests`, rendered inline JS `node --check`, and gunicorn restart all succeeded. Production checks confirmed Haruuru index has `directVoicePlayback: true` while Kanoina remains false; mobile/touch Playwright confirmed the settings button background stays `rgba(248, 248, 248, 0.72)` after tap even though `:hover` remains matched; direct voice test fetched `VOICE/MIS_0001.OGG` as `200 audio/ogg`, SDL AudioContext was `running`, and `render_game_to_text` reported `directVoiceActive: true` with path `/onsyuri/haruuru/voice/mis_0001.ogg`. Required develop-web-game client passed via authenticated proxy; only the existing non-fatal `failed to open nsa or ns2 archive, ignored` console warning appeared, and its canvas-only screenshot remained black due the known WebGL capture limitation.

- Follow-up correction: Haruuru voice was playing twice because the engine now plays the restored `voice` commands and the Haruuru-only direct WebAudio fallback was also active. Disabled Haruuru `directVoicePlayback` so the engine remains the only voice playback path while the dormant fallback code stays available behind the flag.
- Verification/deploy: `manage.py check` and `main.tests.PwaMetadataTests` pass. Restarted production gunicorn and confirmed production `https://www.hanplanet.com/ko/sub/onscripter/haruuru/index.json` returns `directVoicePlayback: false`. Required develop-web-game client passed through the authenticated proxy and `render_game_to_text` reported `directVoicePlayback:false` / `directVoiceActive:false`; only the existing non-fatal `failed to open nsa or ns2 archive, ignored` warning appeared.

- Current prompt: make ONScripter sub-game sound settings default to muted.
- Updated `templates/fun/onscripter_player.html` so the audio panel initial markup starts as muted (`data-muted="true"`, mute button pressed/unmute label) and the JS default `g_onscripter_audio_muted` is `true`. Existing saved `hanplanet.onscripter.audio` localStorage still overrides the default, so user-chosen unmuted/volume settings are preserved.
- Verification/deploy: `manage.py check`, `main.tests.PwaMetadataTests`, rendered inline JS `node --check`, and production gunicorn restart all succeeded. Production HTML confirms the muted initial markup/default JS. Required develop-web-game client via authenticated proxy reported `audioMuted:true` in a fresh browser context. Additional production Playwright check confirmed no saved state -> `audioMuted:true`/volume 80, saved unmuted state -> `audioMuted:false`/volume 50. Only the existing non-fatal `failed to open nsa or ns2 archive, ignored` warning appeared.

- Current prompt: add a save button above the ONScripter skip button, save to data1, and use the existing HanDrive save icon style.
- Added `#onscripter_save_slot_one` above skip/auto in the vertical bottom-right playback control group, using the same floppy-save SVG style already used in HanDrive controls. The button drives the engine's in-game save flow: right-click menu -> SAVE -> slot 1/data1 -> YES. The actual OnscripterYuri saved file for data1 is `save1.dat`.
- Fixed Haruuru's custom R-menu hit target by reading the `lsp ... SAVE` coordinate from `0.txt` before falling back to the default `rmenu` layout calculation; the earlier default calculation landed on the custom `LOAD` row. Also made `flush_save()` return/reuse a Promise so the button waits for IDBFS + remote save upload before reporting `saved` without duplicating the engine-triggered upload.
- Verification/deploy: `manage.py check`, `main.tests.PwaMetadataTests`, rendered inline JS `node --check`, and production gunicorn restart all succeeded. Production Playwright on `https://www.hanplanet.com/ko/sub/onscripter/haruuru/` confirmed the save button appears above skip/auto, `saveSlotOneStatus:"saved"`, and one `remote save uploaded 2` log. The test user's server archive at `_web_saves/haruuru/user-dbdc006002b83e91ea9affa6/save.zip` contains `save1.dat` and `gloval.sav`. Required develop-web-game client also passed with the existing non-fatal `nsa/ns2 archive` warning.

- Current prompt: normalize Speaki character voice volumes, remove delayed voice starts, and prevent defeat voices from playing twice.
- Updated both Speaki multiplayer clients to preload and Web Audio-decode character voice clips when the game is started. Decoded clips are RMS-normalized with peak-safe gain limits and played from memory, falling back to HTMLAudio only until a clip is ready or on browsers that cannot decode it.
- Defeat-caused deaths now retain the defeat voice and suppress the overlapping death voice, for both local and remote players in Bumpercar Spiky and Raise Speaki.
- Verification: `node --check` passes for both clients, `manage.py check` passes, and `collectstatic --noinput` refreshed the local served scripts. The required Playwright game-client smoke tests started and moved through both games with no console/page errors; inspected gameplay screenshots show both canvases rendering normally. A focused Playwright check confirmed all 60 expected character-MP3 URLs are requested successfully on start in each game, with no failed audio requests or console errors.

- Follow-up: source audio measurements showed a roughly 10dB average-level spread, so the peak-safe normalization ceiling was raised to 2.8x; this lets the quietest dialogue reach the common target without clipping. Re-ran syntax/Django/static checks and both required Playwright game-client smokes on a dedicated local port after an unrelated port-8000 tunnel began returning static-file 404s. Inspected both final gameplay screenshots; the focused check again confirms 60/60 character clips requested, with no failed audio requests or console errors.

- Current prompt: remove the multiplayer death modal's shared modal classes; keep only background dimming with no modal background or exterior shadow.
- Removed the `site-modal-*` classes and popup-fit behavior from the shared multiplayer death-modal template. The death backdrop now owns the 24% black dim layer without blur, while the dialog has no background or box shadow.
- Removed `multiplayer-death-modal` from the common popup shadow selector so the common modal exterior shadow cannot be reapplied. This applies to both Bumpercar Spiky and Raise Speaki because they share the template and multiplayer CSS.
- Verification: targeted modal-source tests, `manage.py check`, and `collectstatic --noinput` pass. Focused Playwright checks in both games report no common modal classes, transparent dialog background, `box-shadow: none`, a non-transparent black backdrop, and no console/page errors; inspected screenshots confirm the result. Required game-client smoke tests also passed for both games.

- Follow-up: the earlier defeat-audio deduplication incorrectly skipped every `die` sound when the collision visual was `defeat`. Restored the normal one-time death-transition audio for both self and remote players in Bumpercar Spiky and Raise Speaki; the existing transition guards still prevent repeated `die` playback while `deathActive` remains true.
- Added a source regression test to ensure neither multiplayer client reintroduces a defeat-based death-audio suppression guard. Verification: both JS files pass `node --check`; the regression test, `manage.py check`, and `collectstatic --noinput` pass. The required Playwright game-client smokes render active gameplay normally in both games with no console/page errors.

- Current prompt: reopen the multiplayer start overlay with Escape during gameplay, use a Continue button to close it, and reconnect with the newly selected character only when the skin changed.
- Added shared start/continue labels. Escape now closes an open skin chooser first; otherwise, while alive and in-game it opens the start overlay, clears active movement/boost input, and changes the primary button text to `Continue`. Continue with no skin change only hides the overlay.
- Skin selection now keeps the currently connected skin separate from the pending selection. If the pending skin differs when Continue is pressed, the existing `connect()` path closes the current WebSocket, obtains a token for the new skin, and reconnects; unchanged selections do not reconnect.
- Verification: JS syntax checks, targeted Django source regression tests, `manage.py check`, and `collectstatic --noinput` pass. Focused Playwright checks in Bumpercar Spiky and Raise Speaki confirm `Start! → Esc → Continue → overlay closed` with no console/page errors; inspected Continue screenshots and the required game-client gameplay smoke screenshots both render normally.
- Follow-up verification: Playwright WebSocket events confirm unchanged `Continue` does not create a second socket in either game. The skin-change path is source-regression-tested to call the existing `connect()` only when the pending skin differs from the connected skin.

- Current prompt: let Escape activate Start/Continue while the multiplayer start overlay is open; cancel a pending character change and close that overlay on death; use a dim-only in-game overlay; and return to the initial screen when either idle-timeout modal close button is pressed.
- Both multiplayer clients now route Escape through the existing Start/Continue handler whenever the start overlay is already visible. Death transitions restore the server-connected skin selection, close the skin chooser, and close the start overlay. The in-game overlay gets a dedicated 24%-black, no-blur state while the initial Start screen preserves its existing background treatment.
- Reworked idle-timeout close handling so both the header X and primary close button stop the active socket/timers and return to the initial `Start!` screen without reconnecting.
- Verification: both JS files pass `node --check`; targeted multiplayer source tests, `manage.py check`, `git diff --check`, and `collectstatic --noinput` pass. Required game-client smoke tests render active gameplay normally in both games. Focused Playwright confirms initial `Esc` starts, in-game `Esc` opens `Continue`, another `Esc` continues, the computed in-game backdrop is `rgba(15, 23, 42, 0.24)` with `backdrop-filter: none`, and both idle close buttons return to `Start!` with no console/page errors. Reviewed the resulting gameplay, Continue-menu, and initial-reset screenshots.

- Diagnostic: the multiplayer rotation and joystick-toggle buttons still exist, but `updateCompactViewportUI()` deliberately hides their shared top-action container unless the viewport's shorter side is 900px or less. The joystick itself also begins hidden until its toggle is pressed (or rotation/fullscreen is entered). Local browser inspection confirmed the two controls are hidden at 1920×1080 and visible at 1280×720; no code change was made.

- Current prompt: add an on-screen Escape button using the shared settings icon for the Speaki games.
- Added a third compact-screen top action that reuses `media/icons/settings-gears.svg`, with localized `메뉴 (ESC)` / `Menu (Esc)` accessibility text. Its click action now shares the keyboard Escape state path: close skin chooser, start/continue when the start overlay is open, or open the in-game Continue menu. The button uses the existing active state while that menu is open.
- Verification: both JS files pass `node --check`; targeted multiplayer regression tests, `manage.py check`, `git diff --check`, and `collectstatic --noinput` pass. Focused Playwright checks at 1280×720 confirm three top actions, the shared settings asset, and the click path `gameplay → menu → Continue`; reviewed Bumpercar and Raise Speaki Continue-menu screenshots with no console/page errors. Required game-client smoke runs also completed for both games with no error artifacts.
